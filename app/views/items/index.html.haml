%h1= 'Pending Item Requests'

%table.table
    %thead
        %tr
            %th= 'Title'
            %th= 'Description'
    %tbody
    - @items.each do |item|
        %tr
            %td= link_to item.title, item_path(item.id)
            %td= item.description


